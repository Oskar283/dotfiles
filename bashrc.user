# If not running interactively, do not do anything
[[ $- != *i* ]] && return

# Use vim as default editor. Only add some user settings to speed it up
# whille still having a little config
export EDITOR="vim -u ~/.vimrc_cli"

# Set Swedish keymap
setxkbmap se

# Disable Ctrl-S (Stop terminal output) and Ctrl-q (Start terminal output)
# Old setting that is on by default. Usually just very annoying
stty -ixon

source ~/.bash_aliases
# Bash functions based on already existing aliases

# Usage example:
# ttcn_compile_copy_run <command>
# I.e:
# ttcn_compile_copy_run roadrunner -l /lab/epg_ft/users/eoskrod/no_rename_after_rebase/ --filter-testcases=TC21558__pdn_connection_without_session_id

# Set PS1
export PS1="\[\e[1m\]\h $PWD> \[\e[00m\]\n$"
